import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {},
  row: {
    flexDirection: "row",
    marginVertical: 6,
  },
  txt: {
    fontSize: 20,
  },
});

export default styles;
