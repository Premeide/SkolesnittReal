import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 10,
  },
  title: { fontSize: 25, fontWeight: "bold" },

  paragraph: { fontSize: 20, fontWeight: "400", paddingBottom: 30 },
});

export default styles;
